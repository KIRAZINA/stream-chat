<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Production profile: JSON structured logging -->
    <springProfile name="prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdc>true</includeMdc>
                <customFields>{"service":"stream-chat"}</customFields>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="com.streamchat" level="DEBUG"/>
    </springProfile>

    <!-- Dev/Docker profile: human-readable logging -->
    <springProfile name="dev, docker, default">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="com.streamchat" level="DEBUG"/>
    </springProfile>
</configuration>
